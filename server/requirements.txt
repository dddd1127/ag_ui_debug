# AG-UI 协议调试器后端依赖
# 
# 说明：
# 这些依赖可以复用 agentscope-runtime 项目的虚拟环境
# 如果已在 agentscope-runtime-main/.venv 环境中，可以直接使用该环境
# 
# 核心依赖：
# - fastapi: FastAPI 框架
# - uvicorn: ASGI 服务器
# - pydantic: 数据模型验证
# - agentscope: Agent 框架
# - ag-ui-protocol: AG-UI 协议包
# - fakeredis: 模拟 Redis（用于 session）

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.11.7
# 关键：必须钉死 agentscope 1.x 具体版本。
# 2.x 将 ToolUseBlock 改名为 ToolCallBlock，本项目代码基于 1.x 的 ToolUseBlock，
# 装 2.x 会导致 ImportError。用 == 消除 pip 解析歧义，避免被装成 2.x。
agentscope==1.0.21
# 关键：agentscope 1.x 依赖 mcp，但没设上限，会被装成 mcp 2.0。
# mcp 2.0 移除了 streamablehttp_client，导致 agentscope 导入失败。
# 锁在 <2.0.0，1.29.0 是最后一个含 streamablehttp_client 的版本。
mcp>=1.13,<2.0.0
ag-ui-protocol>=0.1.10
fakeredis>=2.31.0
python-dotenv>=1.0.1
